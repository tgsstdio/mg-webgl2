namespace Magnesium {
  export class WGLFramebuffer implements IMgFramebuffer {
    destroyFramebuffer(
      device: IMgDevice
      , allocator: IMgAllocationCallbacks|null
    ) : void {
			
		}
  }
}